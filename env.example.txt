# AutoQuote AI Environment Variables
# Copy this file to .env.local and fill in your values
# Rename this file to .env.local (remove .txt)

# ============================================================
# Auth0 Configuration (SDK v4 format)
# ============================================================

# Secret for session encryption (min 32 characters)
# Generate with: openssl rand -hex 32
AUTH0_SECRET=your-auth0-secret-here

# Your Auth0 tenant domain (WITHOUT https:// scheme!)
# Example: your-tenant.us.auth0.com
AUTH0_DOMAIN=your-tenant.auth0.com

# Auth0 Application credentials (from Auth0 dashboard)
AUTH0_CLIENT_ID=your-client-id
AUTH0_CLIENT_SECRET=your-client-secret

# Your application base URL (renamed from AUTH0_BASE_URL in v4)
# For local dev: http://localhost:3000
# For production: https://your-app.vercel.app
APP_BASE_URL=http://localhost:3000

# ============================================================
# OpenRouter + Claude Configuration
# ============================================================

# OpenRouter API key (get from openrouter.ai)
OPENROUTER_API_KEY=sk-or-your-api-key-here

# Model to use (MUST be Claude Opus 4.5)
OPENROUTER_MODEL=anthropic/claude-opus-4.5

# Token budget for Claude's extended thinking feature
CLAUDE_THINKING_BUDGET_TOKENS=10000

# ============================================================
# Freepik Configuration
# ============================================================

# Freepik API key for image-to-prompt
FREEPIK_API_KEY=your-freepik-api-key

# Webhook URL for Freepik callbacks (your deployed URL)
FREEPIK_WEBHOOK_URL=https://your-app.vercel.app/api/webhooks/image-to-prompt

# ============================================================
# Vapi Configuration
# ============================================================

# Vapi API key
VAPI_API_KEY=your-vapi-api-key

# Vapi phone number ID for outbound calls
VAPI_PHONE_NUMBER_ID=your-phone-number-id

# Secret for validating Vapi webhooks
VAPI_WEBHOOK_SECRET=your-vapi-webhook-secret

# ============================================================
# ElevenLabs Configuration
# ============================================================

# ElevenLabs voice ID (Rachel is default)
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# ============================================================
# AWS Configuration
# ============================================================

# AWS Region
AWS_REGION=us-east-1

# AWS IAM credentials
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# DynamoDB table names
DYNAMODB_TABLE_SESSIONS=autoquote-sessions
DYNAMODB_TABLE_CALLS=autoquote-calls

# S3 bucket for media uploads
S3_BUCKET_MEDIA=your-autoquote-media-bucket

# ============================================================
# DEMO Safety Configuration (MANDATORY)
# ============================================================

# CRITICAL: Set to "true" for demos and testing
# When true, calls only go to numbers in DEMO_TO_NUMBERS
DEMO_MODE=true

# Comma-separated list of allowed test phone numbers
# Format: +1XXXXXXXXXX (E.164)
DEMO_TO_NUMBERS=+15551234567,+15559876543

# Strategy for selecting demo numbers
# Options: "round-robin" (random) or "first" (always first number)
DEMO_NUMBER_STRATEGY=round-robin

# When true, throws error for non-demo numbers
# When false, replaces them with demo numbers
SCOPE_CALLS_TO_DEMO_LIST=true

# Master switch for outbound calls
ALLOW_OUTBOUND_CALLS=true

# ============================================================
# Application Configuration
# ============================================================

# Your deployed application URL (for webhooks)
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# Enable demo mode banner in UI
NEXT_PUBLIC_DEMO_MODE=true
